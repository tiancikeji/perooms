<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :roomDescription %><br />
    <%= f.text_field :roomDescription %>
  </div>
  <div class="field">
    <%= f.label :rateCode %><br />
    <%= f.text_field :rateCode %>
  </div>
  <div class="field">
    <%= f.label :roomTypeCode %><br />
    <%= f.text_field :roomTypeCode %>
  </div>
  <div class="field">
    <%= f.label :hotel_id %><br />
    <%= f.number_field :hotel_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
